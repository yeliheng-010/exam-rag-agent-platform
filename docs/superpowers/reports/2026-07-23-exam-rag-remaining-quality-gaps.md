# 考试 RAG 剩余质量缺口验收报告

**日期：** 2026-07-23
**范围：** E01 英语赛事表格召回，以及 M25-05、M25-06、M25-08 正式答案结构化
**结论：** 25 条联合评测的已知召回、结构化解析与答案覆盖缺口均已补齐；本轮不推广 `auto` 到正式知识库。

## 实现边界

- 保留现有检索阈值和子 Chunk 排名，只修复结果组装阶段过滤父块的问题。
- `parent_text` 仅可作为 `MatchTypeParentChunk` enrichment 返回，不能作为直接检索或其他 enrichment 类型绕过过滤。
- 考试 resolver 把父块稳定放到对应子块之后，避免 Go map 遍历改变上下文排名。
- 五条答案均从正式源图片人工核验，不从评测金标反推；未修改题干、选项、Chunk、题组关联或评测案例。

## 正式答案复核

| 题号 | 结构化答案 |
| --- | --- |
| 13 | `2` |
| 14 | `$\frac{61}{25}$ (2.44)` |
| 18(1) | `$\frac{x^2}{9}+y^2=1$` |
| 18(2)(i) | `$(\frac{3m}{m^2+(n+1)^2},\frac{n+2-m^2-n^2}{m^2+(n+1)^2})$` |
| 18(2)(ii) | `$3(\sqrt{3}+\sqrt{2})$` |

数据库回读确认五条 `question_answers` 与题号一一对应，`question_explanations` 均包含“图片识别答案”和对应的 `local://` 原始图片 URI。写入事务使用旧值 MD5 作为前置条件。

## 真实运行

最终 `app` 镜像：`sha256:f765a572430607958e394191a75640945501fb76bbf3ec8b86e963fb4183166f`，容器状态为 `healthy`。

| 指标 | Legacy | Auto | Auto Repeat |
| --- | ---: | ---: | ---: |
| 运行 ID | `e451a9a0-9762-4907-ba16-d739727ce2f1` | `b22de532-42e9-4e4f-be7d-99b77ebf78f4` | `9d80097b-8f1c-4cf3-af0b-39589ec4445c` |
| 案例数 | 25 | 25 | 25 |
| Recall@20 | 1.00 | 1.00 | 1.00 |
| 结构化解析率 | 1.00 | 1.00 | 1.00 |
| 答案覆盖率 | 1.00 | 1.00 | 1.00 |
| 总体通过率 | 1.00 | 1.00 | 1.00 |
| MRR | 0.165799 | 0.201640 | 0.201633 |
| 平均耗时 | 914.88 ms | 896.20 ms | 913.64 ms |

三次运行的核心逐案例结果签名均为 `7e231b231ece8f700560a0f34abced6e`。两次 auto 的同分 Chunk 次序仍可能不同，因此 MRR 末位和完整 rank signature 有轻微波动，但命中、题组关联、答案判定和所有验收指标一致。

## 关键案例

- E01：两次 auto 的首个相关排名均为 `2`，关联题组为 `6adc1f07-6eb8-48e7-aa77-9733328c1cfc`，召回与答案均通过。
- M25-05：关联题组 `4b931c78-f4c9-4fc8-9b7b-a3172da060f0`，答案 `2`，通过。
- M25-06：关联题组 `459c3a15-9f01-41e6-b92d-e8ee8d50897a`，答案 `$\frac{61}{25}$ (2.44)`，通过。
- M25-08：关联题组 `30a594f3-3864-44f3-bfd6-d973e374cc0b`，三个小题答案均结构化，通过。

## 只读性与浏览器验收

- `question_chunk_refs` 前后均为 240 行；按固定字段排序得到的摘要前后均为 `c182a2a0b9fbedc880dd5c85fe4c02de`。
- 桌面 `1440x1000`：页面显示 25/25，核心覆盖指标均为 100%；目标案例可展开，E01 首个相关排名为 2；文档宽度等于视口宽度。
- 移动端 `390x844`：指标、运行历史、案例列表和候选表格正常换行；文档宽度为 390px，活动工作区无越界元素。
- 两个视口的浏览器控制台均无 error。

## 审查与验证

- 手工审查父块 enrichment、直接检索过滤、父子排序和去重边界，未发现实现级缺陷。
- 补充测试锁定直接检索和关系 enrichment 均不能返回 `parent_text`。
- Go 相关六包、前端两组定向测试及生产构建均通过；生产文件保持在 300 行以内。

## 决策

本轮证明 legacy 与 auto 都能达到相同的满分质量门槛，但没有形成足以推广 `auto` 的 A/B 优势。正式知识库保持现状，不执行重切、不修改正式引用、不推广候选策略。
